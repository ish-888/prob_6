#include <stdio.h>// checking divisibility
int main(){
    int a =33950;
    printf("The value of a is %d",a%97); //the value given is divisble by 97 and hence the output value will be 0.
    return 0;
}
